const { cors, publicWs } = require('../lib/deriv');
module.exports=async(req,res)=>{cors(res);if(req.method==='OPTIONS')return res.status(204).end();try{const d=await publicWs({active_symbols:'brief',product_type:'basic'});const raw=d.active_symbols||d.data||[];const markets=raw.map(x=>({symbol:x.underlying_symbol||x.symbol,display:x.underlying_symbol_name||x.display_name||x.symbol,type:x.underlying_symbol_type||x.symbol_type,pip_size:x.pip_size})).filter(x=>x.symbol);res.setHeader('Cache-Control','s-maxage=5, stale-while-revalidate=15');return res.json({markets});}catch(e){return res.status(502).json({error:e.message});}};
