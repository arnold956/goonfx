const { getToken, derivRest, cors } = require('../lib/deriv');
module.exports=async(req,res)=>{cors(res);if(req.method==='OPTIONS')return res.status(204).end();const token=getToken(req);if(!token)return res.status(401).json({error:'Not connected to Deriv'});try{const d=await derivRest('/trading/v1/options/accounts',token);const accounts=(d.data||d.accounts||[]).map(a=>({account_id:a.account_id||a.id,mode:a.account_type||a.mode||'demo',balance:a.balance,currency:a.currency}));const current=accounts[0]||null;return res.json({accounts,current});}catch(e){return res.status(502).json({error:e.message});}};
