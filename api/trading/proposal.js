const {getToken,derivRest,cors,jsonBody}=require('../../lib/deriv');
async function wsRequest(url,payload,match){return new Promise((resolve,reject)=>{const ws=new WebSocket(url);const timer=setTimeout(()=>{try{ws.close()}catch{};reject(new Error('Deriv trading request timed out'))},10000);ws.onopen=()=>ws.send(JSON.stringify(payload));ws.onmessage=e=>{let d;try{d=JSON.parse(e.data)}catch{return}if(d.error){clearTimeout(timer);try{ws.close()}catch{};return reject(new Error(d.error.message||'Deriv trading error'))}if(!match||match(d)){clearTimeout(timer);try{ws.close()}catch{};resolve(d)}};ws.onerror=()=>{clearTimeout(timer);reject(new Error('Deriv trading connection failed'))}})}
module.exports=async(req,res)=>{cors(res);if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const token=getToken(req);if(!token)return res.status(401).json({error:'Not connected to Deriv'});try{const b=await jsonBody(req);if(!b.account_id||!b.params)return res.status(400).json({error:'account_id and params are required'});const otp=await derivRest(`/trading/v1/options/accounts/${encodeURIComponent(b.account_id)}/otp`,token,{method:'POST',body:'{}'});const url=otp.data?.url||otp.url;if(!url)throw new Error('Deriv did not return a trading session URL');const params={...b.params,proposal:1,req_id:Date.now()%1000000};const d=await wsRequest(url,params,x=>x.msg_type==='proposal');return res.json({proposal:d.proposal});}catch(e){return res.status(502).json({error:e.message||'Proposal failed'});}};